#include <fstream>

using namespace std;
int F[101], N;

int main() {
    ifstream in("phan-tich-so.inp");
    ofstream out("phan-tich-so.out");

    for(int k = 1; k <= n; ++k)
        F[k] = 0;
    F[0] = 1;

    for (int m = 1; m <= n; ++m) 
        for (int v = 0)
}