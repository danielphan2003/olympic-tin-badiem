#include <iostream>
#include <deque>
#include <vector>

using namespace std;

void solve() {
    int N, k;
    deque<int> deq;

    cin >> N >> k;
    vector<int> A(N+1, 0);

    for (int i = 1; i <= N; i++) {
        cin >> A[i];
    }

    for (int i = 1; i <= N; i++) {
        while (!deq.empty() && a[i] )
        deq.
    }
}

int main() {
    int T;
    cin >> T;

    for (; T >= 0; T--) {
        solve();
    }
}